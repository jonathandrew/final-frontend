import React from "react";
import Button from "./Button";

const AddingSpell = () =>
  // { spell: { spell, effect } }
  {
    return (
      <div>
        <Button>Hello</Button>
      </div>
    );
  };

export default AddingSpell;
